source 'https://rubygems.org'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in omniauth-potok.gemspec
gemspec

group :development do
  gem 'rubocop', require: false
end

group :test do
  gem 'rspec'
  gem 'simplecov'
  gem 'webmock'
end
